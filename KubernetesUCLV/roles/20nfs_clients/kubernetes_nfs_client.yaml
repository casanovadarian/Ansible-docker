---
- name: Instalar cliente NFS
  hosts: kubernetes
  become: true

  tasks:

    - name: Instalar nfs-common
      ansible.builtin.apt:
        name: nfs-common
        state: present
        update_cache: true

